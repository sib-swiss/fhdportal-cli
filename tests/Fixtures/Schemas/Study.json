{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Study",
    "description": "Minimal Study schema fixture for testing (not a production schema).",
    "required": ["name"],
    "properties": {
        "name": {
            "type": "string",
            "minLength": 1,
            "description": "Unique study name"
        },
        "description": {
            "type": "string"
        }
    },
    "additionalProperties": true,
    "x-resource": {
        "schema": {
            "fields": [
                {
                    "name": "name",
                    "constraints": {
                        "required": true
                    }
                },
                {
                    "name": "description"
                }
            ],
            "primaryKey": ["name"]
        }
    }
}
